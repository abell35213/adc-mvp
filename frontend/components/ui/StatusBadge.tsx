import type { ReactNode } from "react";import { statusBadgeClass, type StatusTone } from "@/lib/design/tokens";import { cn } from "@/lib/design/utilities";export function StatusBadge({tone="neutral",children,icon,dot=false,accessibleLabel,className}:{tone?:StatusTone;children:ReactNode;icon?:ReactNode;dot?:boolean;accessibleLabel?:string;className?:string}){return <span aria-label={accessibleLabel} className={cn(statusBadgeClass(tone),className)}>{dot&&<span aria-hidden="true" className="size-1.5 rounded-full bg-current"/>}{icon}<span>{children}</span></span>}
