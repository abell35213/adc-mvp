import type { ReactNode } from "react";import { cn } from "@/lib/design/utilities";export function EmptyState({title,message,icon,primaryAction,secondaryAction,variant="compact",className}:{title:ReactNode;message:ReactNode;icon?:ReactNode;primaryAction?:ReactNode;secondaryAction?:ReactNode;variant?:"compact"|"full";className?:string}){return <div className={cn("rounded-lg border border-dashed border-border-default bg-surface text-center",variant==="full"?"px-8 py-16":"px-6 py-8",className)}>{icon&&<div className="mx-auto mb-3 flex size-10 items-center justify-center rounded-full bg-surface-subtle text-text-muted">{icon}</div>}<h3 className="text-base font-semibold text-text-primary">{title}</h3><p className="mx-auto mt-2 max-w-md text-sm text-text-secondary">{message}</p>{(primaryAction||secondaryAction)&&<div className="mt-5 flex flex-wrap justify-center gap-2">{primaryAction}{secondaryAction}</div>}</div>}
